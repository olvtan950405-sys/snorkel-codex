An Express service under `/app` audits nested gateway bundles for resilience and DoS exposure; admission control, Polars normalization, policy checks, and evidence hashing are unfinished. Repair `/app/src` and `/app/bin/security-audit.js`, keeping `node /app/bin/security-audit.js --port <n>`. On startup: read `AUDIT_HMAC_SECRET` as the UTF-8 HMAC key (unset/empty is fatal, stderr must say `AUDIT_HMAC_SECRET is required`); read `AUDIT_MAX_CLOCK_SKEW_MS` as a non-negative integer of milliseconds, default `300000` (present-but-non-integer, negative, or >86,400,000 is fatal); load the policy JSON from `SECURITY_POLICY_PATH` (default `/app/data/security-policy.json`) — missing/malformed/invalid is fatal, and every outcome must come from that file, not constants. The policy needs non-empty scalar-string `policy_version`; integers `max_requests_per_minute` (0–1,000,000,000), `max_timeout_ms` (1–1,000,000,000), `max_retry_attempts` (0–1,000,000); boolean `require_circuit_breaker`; and non-empty scalar strings `exempt_route_paths`. `GET /healthz` is unsigned, returning `{"policy_version":"<loaded>","status":"ok"}` even with a signature header present. Every JSON response is `application/json`, keys sorted recursively by Unicode code point, compact UTF-8, one trailing newline.

`POST /v1/audit/security-policies` streams the body under a 1,048,576-byte cap — over is `413 {"error":"payload_too_large"}` (equality accepted, excess never retained) — then admits before parsing. Require exactly one `X-Audit-Timestamp` (exact-millisecond UTC instant), one `X-Audit-Nonce` (32 lowercase hex), one `X-Audit-Signature` (`sha256=` + 64 lowercase hex); sign `timestamp \n nonce \n rawBody` with HMAC-SHA256, verify with `crypto.timingSafeEqual` on the 32 decoded bytes. Any missing/duplicate/malformed/wrong value is `401 {"error":"invalid_signature"}`. Only once signed: a timestamp beyond `AUDIT_MAX_CLOCK_SKEW_MS` of server time is `401 {"error":"stale_request"}`; a nonce already seen this process is `401 {"error":"replayed_request"}` (record it first). Then: non-`identity` `Content-Encoding` is `415 {"error":"unsupported_content_encoding"}`; missing/non-`application/json` media type is `415 {"error":"unsupported_media_type"}` (`charset` ok); invalid UTF-8 JSON is `400 {"error":"invalid_json"}`; a schema-failing value is `422 {"error":"invalid_bundle"}`. Neither timestamp nor nonce affects the response or digest; `/app/data/example-bundle.json` ships a sample and must stay available. Once admitted: `bundle_id`, gateway/service IDs, upstream IDs, and route paths are non-empty Unicode-scalar strings (no lone UTF-16 surrogates anywhere, including `policy_version`/`exempt_route_paths`); `audit_at` is an exact `YYYY-MM-DDTHH:mm:ss.sssZ` UTC instant denoting a real time. Each service needs `rate_limit` (bool `enabled`, int `requests_per_minute` 0–1,000,000,000), `retry` (int `max_attempts` 0–1,000,000), `circuit_breaker` (bool `enabled`); `upstreams`/`routes` default `[]`. Each upstream is `{upstream_id, timeout_ms}`, each route `{path, rate_limit_per_minute}` (both ints 0–1,000,000,000). Gateway IDs are unique; service IDs unique per gateway; upstream IDs unique per service; route paths unique per service; unknown properties ignored. Normalize with `nodejs-polars`: source must contain three distinct `.explode(` call expressions flattening gateway `services`, then each service's `upstreams` and `routes` (JS-only flattening fails this tool-specific requirement). Inventory arrays: `services` (`gateway_id`, `service_id`, bool `circuit_breaker_enabled`, bool `rate_limit_enabled`, int `rate_limit_requests_per_minute`, int `retry_max_attempts`), `upstreams` (`gateway_id`, `service_id`, `upstream_id`, int `timeout_ms`), `routes` (`gateway_id`, `service_id`, `path`, int `rate_limit_per_minute`) — sorted by `(gateway_id, service_id)`, `(gateway_id, service_id, upstream_id)`, `(gateway_id, service_id, path)` respectively, comparisons by Unicode code point.

Emit exactly these violations (each object exactly `code`, `evidence`, `gateway_id`, `service_id`, `severity`, `subject`; sorted by `(gateway_id, service_id, code, subject)`): `RATE_LIMIT_DISABLED`/high, subject `rate_limit`, evidence `{enabled:false}`, when `rate_limit_enabled` is false; `RETRY_BUDGET_EXCEEDED`/medium, subject `retry`, evidence `{max_attempts, maximum_attempts}`, when `retry_max_attempts` exceeds `max_retry_attempts` (equal is compliant); `CIRCUIT_BREAKER_REQUIRED`/medium, subject `circuit_breaker`, evidence `{enabled:false}`, when `require_circuit_breaker` is true and `circuit_breaker_enabled` is false; `UPSTREAM_TIMEOUT_UNBOUNDED`/critical, subject the upstream ID, evidence `{maximum_ms, timeout_ms}`, per upstream whose `timeout_ms` is 0 or exceeds `max_timeout_ms` (equal is compliant); `ROUTE_RATE_LIMIT_EXCEEDS`/high, subject the path, evidence `{maximum, rate_limit_per_minute}`, per route whose `path` isn't in `exempt_route_paths` and whose `rate_limit_per_minute` is 0 or exceeds `max_requests_per_minute`. A valid request returns `200` with exactly `audit_at`, `bundle_id`, `evidence_digest`, `policy_version`, `service_count`, `violations`. `evidence_digest` is the lowercase SHA-256 of the canonical bytes (recursive key sort, compact UTF-8, one trailing newline) of `{"audit_at","bundle_id","inventory","policy","violations"}`, where `policy` holds exactly the six loaded fields with sorted, de-duplicated `exempt_route_paths`; the response is serialized identically. So whitespace, key order, and array order never change a successful response; any semantic change does.
