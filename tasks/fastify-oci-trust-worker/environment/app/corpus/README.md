# Incident corpus snapshot

This directory is the build-time snapshot location for `terminal-bench/oci-trust-incident-corpus`. The public seed is intentionally empty; the admission worker must handle records supplied by incident responders and the verifier without network access.
