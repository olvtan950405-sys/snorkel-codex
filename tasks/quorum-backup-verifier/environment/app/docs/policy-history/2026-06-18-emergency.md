# Emergency exception scope — 18 June 2026

An exception is a narrow waiver for a key's time status, not a new trust root. It must match the
tenant, key, manifest time, and literal bundle-ID prefix stored in the catalog. It does not waive a
wrong tenant, unknown role, invalid signature, or any quorum requirement. The interval ends
exclusively.
