Our offline release mirror was partway through a signing-key rotation when its repository auditor was replaced, and the new copy of `repoguard` under `/app` cannot be trusted. It produces plausible reports, but it has authorized releases from rolled-back metadata, counted duplicate signatures toward thresholds, and advanced its trust database before discovering bad target content.

Repair `repoguard` so `/app/bin/repoguard reconcile` validates the repository at `/app/repository` against `/app/state/trusted-root.json` and `/app/state/trust.db`, then writes the enforcement artifacts under `/app/out`. The tool must authenticate the root rotation and the timestamp/snapshot/targets chain, apply delegation precedence, verify target bytes, and update monotonic trusted versions only after the complete reconciliation succeeds. `/app/docs/repository-contract.md` defines the metadata profile, validation order, delegation rules, artifact formats, and CLI contract; treat it as authoritative. Keep the implementation in the Rust sources under `/app/src` and leave the tool runnable through `/app/bin/repoguard` for any repository and state paths supplied by its documented environment variables.
