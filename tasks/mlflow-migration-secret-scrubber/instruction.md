The legacy experiment recorder left a captured MLflow request stream at `/app/fixtures/legacy-requests.jsonl`. Repair the Go migration gateway under `/app` so `/app/bin/mlflow-gateway scrub --input <jsonl> --output <jsonl>` classifies every request and produces a safe, deterministic forwarding record for each input line. The exact supported MLflow routes, normalization, rejection precedence, recursive secret handling, URL sanitization, and output schema are specified in `/app/docs/scrubbing-contract.md`.

Treat this as a security boundary: credentials and sensitive MLflow tag values must never survive in either accepted forwarding records or rejection records, including secrets embedded in presigned URLs and nested JSON. Keep the implementation in the provided Go module, make it work for arbitrary conforming streams rather than only the captured fixture, and rebuild `/app/bin/mlflow-gateway` from those sources.
