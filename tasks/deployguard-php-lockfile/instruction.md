The deploy webhook service under `/app` was left vulnerable after a signing-key rollover. Repair the PHP implementation so `POST /v1/deploy/authorize` follows `/app/docs/deploy-webhook-contract.md`: authenticate the exact request bytes with the database-selected HMAC key, enforce the timestamp and key-validity windows, validate and fingerprint the submitted `composer.lock`, apply the environment policy, and claim accepted nonces atomically in SQLite.

Keep the public command `/app/bin/deployguard serve --host <host> --port <port> --database <sqlite>`. Handle arbitrary conforming inputs rather than only the shipped example. Rejected requests must not consume nonces, simultaneous uses of one nonce must produce at most one authorization, and no signing secret may appear in a response or log.
